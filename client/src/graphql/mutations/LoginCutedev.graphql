mutation LoginCutedev($password: String!, $username: String!) {
  login(password: $password, username: $username) {
    cutedev {
      id
      username
    }
    errors {
      field
      message
    }
  }
}
